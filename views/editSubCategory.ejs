

  <div>
    <%- include('header'); -%>

<div class="row mt-5">
    <div class="col-md-6 m-auto">
      <div class="card card-body">
        <h1 class="text-center mb-3">
            <i class="fas fa-user-plus"></i> Edit Category
         </h1>
         <form method="post">
       
            
            <div class=" form-group">
              <label for="company" class=" form-control-label">Select Category: </label>
              <select id="category"  name="category_id" class="form-control">
                  <option>Select Category</option>
                  <% for(var i=0; i<item2.length;i++) { %>
                  <option value=<%= item2[i]._id%>><%= item2[i].cname %></option>
                  <% } %>    
              </select>
             
              </div>
         

            <div class="form-group">
                <label for="name">Sub Category</label>
                <input type="text"  value="<%= item.scname %>"  class="form-control" name="scname" required > </td>
            </div>

            <input type="submit" class="btn btn-dark">
            
    </form>            

    </div>
 </div>
</div>

</div>